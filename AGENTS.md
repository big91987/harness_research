# AGENTS.md

## 写作指导

这个仓库既是 Harness Engineering 的实现工程，也是课程化文章归档。
编辑 `specs/` 下的文章时，教学方法只作为内部写作约束，不要在正文里自我暴露。

### 课程口吻

- 以老师解释工程系统的方式直接面向读者写，不写“我们打算怎么教你”。
- 不要在文章正文暴露元策略。避免“费曼学习法”“这是我们的教学策略”“写给初学者”这类说法。
- 优先写清楚具体工程问题：模块解决什么问题、位于架构哪一层、输入是什么、输出是什么、失败时怎样、读者如何验证。
- 避免自我修正式口径，不要让架构看起来像边修边补出来的。分层架构应呈现为稳定地图。
- 语气要清晰、笃定、克制。文章应该像技术课程，不像幕后备忘录，也不像宣传稿。

### Spec 文章要求

- 每篇文章必须引用总览架构：
  `specs/000-总览-Harness-分层架构与连载目录.md`。
- 文件名和 H1 标题必须完全一致，去掉 `.md` 后相同。
- 每篇实现文章都应该包含：
  - 模块在整体架构中的位置；
  - 预期输入、输出、行为和失败模式；
  - 实现原理；
  - 至少一张 Mermaid 流程图；
  - 当前实现状态；
  - 可复制的测试命令；
  - 后续扩展方向。

### 横向对标要求

每篇实现文章不仅要描述本仓库的实现，还要说明当前先进 Harness
在同一模块上的对应做法。默认对标对象包括：

- Claude Code；
- Codex；
- OpenClaw；
- Hermes Agent。

写作时应尽量包含一个“横向对标”小节，至少回答：

- 这个模块在上述 Harness 中分别叫什么，位于哪一层；
- 它们解决的是注册、运行时加载、执行、安全、状态、可观测还是治理问题；
- 它们的关键设计是什么，例如 CLI-first、tool/MCP registry、skill
  runtime、sandbox、context compact、subagent、trace/eval；
- 本仓库当前实现覆盖了哪些点，哪些只做了教学级最小实现，哪些还没做；
- 我们和它们的差异是什么；
- 为什么这里采用当前做法。

不要把横向对标写成营销式优劣排名。重点是帮助读者建立工程坐标系：
不同 Harness 面对同一个模块时，边界如何划分、能力如何落地、取舍来自哪里。

如果当前没有足够证据，不要臆测某个项目的内部实现。可以写“待补证据”，
并标明需要从源码、文档、运行行为或已有分析文章中补齐。

### 开发建议口径

每篇文章应把“为什么这样实现”说清楚。解释可以来自教学取舍，也可以来自
工程取舍，但不要只停留在结果描述。

- 教学取舍：本仓库可以先实现最小可验证版本，用来暴露模块边界和测试方法。
- 工程取舍：对安全、状态、成本、可观测、扩展性有影响的地方，要说明为什么
  先做轻量版本，后续如何演进到生产级。
- 对照取舍：当 Claude Code、Codex、OpenClaw、Hermes Agent 的做法更完整时，
  要说明我们暂时不实现的原因，例如依赖服务化、需要多进程/多节点、需要更
  重的 sandbox、需要 UI/IDE/channel 支撑。
- 边界取舍：注册和运行时加载、memory 和 skill、policy 和 sandbox、trace 和
  eval 这些容易混淆的边界，要明确拆开讲。
- 验证取舍：每个开发建议最好能落到测试、trace、doctor、golden case 或
  live smoke 的验证方式上。

### 优先级口径

- 实现顺序要解释为穿过分层架构的纵向可运行切片，而不是临时修补。
- Tool/MCP Registry 和 Skill/Plugin Registry 是早期控制平面优先级。
- Subagent/Multi-agent 是 runtime、permission、queue、trace 稳定后的关键扩展线。
