[project]
name = "minimal-harness"
version = "0.1.0"
description = "A minimal but complete agent harness with CLI and kernel."
requires-python = ">=3.9"
dependencies = []

[project.scripts]
harness = "harness.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
